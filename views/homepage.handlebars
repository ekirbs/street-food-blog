{{#each posts as |post| }}
<div class="row mb-4 project">
  <div class="col-md-5">
    <h2>
      {{{get_img}}}
      <a href="/project/{{post.id}}">{{post.title}}</a>
    </h2>
    <p>Created by {{post.user_id}}</p>
  </div>
  <div class="col-md-7">
    <p>
      {{post.post_body}}
    </p>
  </div>
</div>
{{/each}}