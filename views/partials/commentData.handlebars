<div class="comments">
  {{#each comment}}

  {{!-- {{#each this}} --}}
  <div class="comment">
    <div class="commentUserInfo">
      {{user.username}}
    </div>
    <div class="comment_body">
      <p{{comment.comment_body}}></p>
      {{!-- <button type="delete" class="delete-post-btn">Delete This Comment</button> --}}
    </div>
  </div>
  {{/each}}
</div>