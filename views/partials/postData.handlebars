 {{!-- <div class="post">
  <div class="title">
    <a href="/post/{{post.id}}">{{post.title}}</a>
    <span class="float-right">Posted by: {{user.username}}</span>
    <br>
    <span class="float-right">Comments: {{comments.length}}</span>
    <br>
    <span class="float-right">Body: {{post.post_body}}</span>
  </div>
  <div class="post-body">
    <p>{{post.post_body}}</p>
  </div>
</div> --}}

<div class="card">
  <div class="card-header card-header-title">

    <div class="column">
      <h3>
        <a href="/post/{{id}}">{{title}}</a>
      </h3>
    </div>

    <div class="column">
      {{!-- <p> Comments: {{comments.length}}</p> --}}
      <p>Posted By: {{user.username}}</p>
    </div>

  </div>

  <div class="card-content">
    <div class="content">
      {{post_body}}
    </div>
    <div>
      {{!-- <b>{{user.username}}</b> --}}
      <a href="/post/{{comment.id}}">{{comments.length}} {{format_plural "comment" comments.length}}</a>
    </div>
  </div>

</div>